---
phase: 24-part-scaffold-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/4-workflow/index.md
  - src/4-workflow/preparation.md
  - src/4-workflow/field-day.md
  - src/4-workflow/data-power.md
  - src/4-workflow/after-return.md
  - src/SUMMARY.md
  - src/introduction.md
  - README.md
autonomous: true

must_haves:
  truths:
    - "독자가 좌측 목차(SUMMARY)와 소개 카드 목차에서 3부와 부록 사이에 '4부 · 현장 워크플로' 파트를 본다"
    - "4부 개요 랜딩(4-workflow/index.md)이 준비→하루→데이터·전원→귀국 후 흐름을 안내하고, 각 트랙 촬영·편집법은 기존 1·2·3부로 링크 승계함을 밝힌다"
    - "파트의 예정 챕터(준비·하루·데이터전원·귀국후)가 모두 정직한 '작성 예정' 스텁으로 존재하고 SUMMARY 항목과 1:1 일치한다(create-missing=false 위반 없음)"
    - "`mdbook build src`가 종료코드 0으로 그린이고 내부 링크 전수 스캔 REAL DEAD가 0이다"
    - "빌드 산출물 docs/는 커밋되지 않고 src/**·README.md만 스테이징된다"
  artifacts:
    - path: "src/4-workflow/index.md"
      provides: "4부 개요 랜딩(실 프로세스 산문, 스텁 아님) — 흐름 안내 + 1·2·3부 링크 승계 선언"
      contains: "현장 워크플로 개요"
      min_lines: 30
    - path: "src/4-workflow/preparation.md"
      provides: "여행 전 준비 스텁(Phase 25 PREP-01/02 예정)"
      contains: "작성 예정"
    - path: "src/4-workflow/field-day.md"
      provides: "하루 현장 운용 스텁(Phase 26 FIELD-01/02 예정)"
      contains: "작성 예정"
    - path: "src/4-workflow/data-power.md"
      provides: "데이터·전원 운용 스텁(Phase 27 DATA-01/02 예정)"
      contains: "작성 예정"
    - path: "src/4-workflow/after-return.md"
      provides: "귀국 후 정리 스텁(Phase 28 POST-01/02 예정)"
      contains: "작성 예정"
    - path: "src/SUMMARY.md"
      provides: "'# 4부 · 현장 워크플로' 섹션(5개 링크) — 3부 끝과 부록 사이"
      contains: "# 4부 · 현장 워크플로"
    - path: "src/introduction.md"
      provides: "4부 카드 그룹(toc-part) — 부록 그룹 앞"
      contains: "4-workflow/index.html"
  key_links:
    - from: "src/SUMMARY.md"
      to: "src/4-workflow/*.md"
      via: "5개 마크다운 링크가 실제 존재 파일을 가리킴(create-missing=false)"
      pattern: "4-workflow/(index|preparation|field-day|data-power|after-return)\\.md"
    - from: "src/introduction.md"
      to: "src/4-workflow/*.html"
      via: "toc-part 카드 그룹(part-title + 5 카드)"
      pattern: "4-workflow/index\\.html"
    - from: "src/4-workflow/index.md"
      to: "1-travel / 2-drone / 3-astro"
      via: "촬영·편집법 승계 링크(재설명 금지)"
      pattern: "\\.\\./(1-travel|2-drone|3-astro)/"
---

<objective>
"4부 · 현장 워크플로" 파트를 3부 뒤·부록 앞에 신설한다. 새 디렉토리 `src/4-workflow/`에 개요 랜딩 1장(실 산문)과 4개 트랙 스텁(준비·하루·데이터전원·귀국후)을 만들고, 이를 좌측 목차(`src/SUMMARY.md`)·소개 카드 목차(`src/introduction.md`)·저장소 README에 배선한다. `create-missing = false`이므로 새 `.md`는 반드시 SUMMARY 항목과 함께 생성해 빌드를 그린으로 유지한다.

Purpose: v1.5 현장 워크플로의 구조 뼈대. 이 파트가 존재해야 Phase 25~28이 각 스텁을 실내용으로 채울 수 있다(구조 기반 페이즈, 요구사항 없음).
Output: 5개 신규 `.md`(랜딩 1 + 스텁 4), SUMMARY 4부 섹션, introduction 4부 카드 그룹, README 4부 항목, 빌드 그린 + 링크 dead 0 게이트 통과.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 스타일 참조(승계 대상 — 톤·구조를 맞출 것)
@src/3-astro/index.md          # 개요 랜딩 산문 톤·흐름 안내 예시
@src/SUMMARY.md                # 파트 섹션 배치 위치
@src/introduction.md           # 카드 목차(toc-part) 패턴
</context>

<constraints>
프로젝트 불변 규칙(반드시 준수):

1. **`src/book.toml`에 `create-missing = false`** — 새 `.md` 5개는 반드시 같은 커밋 범위에서 `src/SUMMARY.md` 항목과 함께 존재해야 한다. SUMMARY에 링크했는데 파일이 없거나, 파일만 있고 SUMMARY에 없으면 빌드가 깨진다(전자) / 고아가 된다(후자). 5개 파일 ↔ 5개 SUMMARY 줄 1:1.
2. **빌드 명령:** `/opt/homebrew/bin/mdbook build src` — 종료코드 0, "not found"/"dead"/broken-link 경고 없이 그린.
3. **`docs/`는 절대 커밋 금지** — CI 생성물. 빌드 실행 전후로 `git restore docs/ 2>/dev/null; git clean -fdq docs/ 2>/dev/null`. 스테이징은 `src/**`와 `README.md` 파일만 개별 `git add`.
4. **정직성(v1.5):** 하루 타임라인 시각 = 예시 템플릿(고정 시각 단정 금지); 장비 무게·카드/파워뱅크 용량·요금 = 검증/미확인 표기; 특정 백업 장비·인버터·SSD·클라우드 = 추천만(정밀 스펙·요금 단정 금지); 실촬영본·화면 캡처·하루 실사례 = 트립(8/13) 이후 교체. 스텁 문구에 해당 트랙과 관련된 정직성 단서를 심는다.
5. **중복 금지:** 각 트랙의 촬영·편집법 자체는 4부에서 재설명하지 않고 기존 1·2·3부 링크로 승계한다.
6. **신규 페이지의 모든 마크다운 링크는 실제 존재 파일만 가리킨다** — 4부 5개 신규 파일 또는 이미 존재하는 1/2/3부·부록 페이지. 아직 없는 하위 앵커/미래 파일로 링크하지 말 것.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: 4부 디렉토리 신설 — 개요 랜딩(산문) + 4개 트랙 스텁 5개 파일 생성</name>
  <files>
src/4-workflow/index.md
src/4-workflow/preparation.md
src/4-workflow/field-day.md
src/4-workflow/data-power.md
src/4-workflow/after-return.md
  </files>
  <action>
`src/4-workflow/` 디렉토리를 만들고 5개 `.md`를 생성한다. 링크는 `4-workflow/` 기준 상대경로이므로 다른 파트로 갈 때는 `../1-travel/...` 형태다.

**(1) `src/4-workflow/index.md` — 개요 랜딩 (스텁 아님, 실 산문 ~40줄+):**
`src/3-astro/index.md`의 톤(따뜻한 도입 → 이 파트가 안내하는 흐름 → 마무리 CTA)을 따르되 4부 성격(가로 연결 조직)을 살린다. 반드시 포함:
- H1: `# 현장 워크플로 개요`
- 도입 산문 1~2문단: 1·2·3부가 "각 트랙을 어떻게 찍고 편집하나"(수직 깊이)를 다뤘다면, 4부는 이를 **하나의 여행으로 잇는 가로 연결**(여행 전 준비 → 하루 현장 운용 → 데이터·전원 운용 → 귀국 후 정리)을 다룬다는 취지.
- `## 이 파트가 안내하는 흐름` 섹션: 4개 트랙을 순서대로 소개하는 번호 목록. 각 항목은 해당 스텁으로 링크:
  1. [여행 전 준비](preparation.md) — 통합 패킹·촬영 계획·오프라인 앱
  2. [하루 현장 운용](field-day.md) — 하루 타임라인·세 카메라(여행/드론/은하수) 오케스트레이션
  3. [데이터·전원 운용](data-power.md) — 카드·현장 백업·멀티데이 전원
  4. [귀국 후 정리](after-return.md) — 3-2-1 백업·선별·편집 순서 허브
- `## 촬영·편집법은 기존 파트로` 섹션(승계 선언): 4부는 **각 트랙의 촬영법·편집법 자체를 재설명하지 않는다**고 명시하고, [1부 · 여행 사진](../1-travel/index.md) · [2부 · 드론 사진·영상](../2-drone/index.md) · [3부 · 천체사진(은하수)](../3-astro/index.md)로 승계 링크한다.
- 정직성 노트(인용 블록): 4부의 하루 사례·타임라인 시각·화면 캡처·용량/요금 수치는 **예시 템플릿이며 검증/미확인 항목은 그렇게 표기하고, 실제 하루 실사례·촬영본은 트립(8/13) 이후 채운다**는 취지 1~2문장.

**(2) 4개 스텁** — 각 스텁은 빈 페이지가 아니라 **정직한 "작성 예정"**: H1 제목 + 1~2문장으로 "이 페이지가 무엇을 다룰지" + "실제 하루 사례·화면 캡처·수치는 트립(8/13) 이후 채운다"는 노트 + (선택) 관련 기존 파트 승계 링크 1개. 각 스텁 하단에 `> 📝 **작성 예정** …` 인용 블록으로 명시. 트랙별 정직성 단서 포함:

- `src/4-workflow/preparation.md` — `# 여행 전 준비`. 다룰 내용: 세 카메라(R7·드론·은하수) 통합 패킹 리스트, 촬영 계획(명소별 시간대·달·일출/일몰), 오프라인 지도·앱 준비. 정직성: 장비 무게·카드/배터리 용량은 검증/미확인으로 표기 예정. 승계 예: 장비 세부는 [부록 · 카메라·렌즈 추천](../appendix/camera-lens-picks.md) / [3부 장비 가이드](../3-astro/1-gear/index.md).
- `src/4-workflow/field-day.md` — `# 하루 현장 운용`. 다룰 내용: 새벽~밤 하루 타임라인(예시 템플릿), 여행·드론·은하수 세 카메라를 하루 안에서 언제·어떻게 굴리나(오케스트레이션). 정직성: 타임라인 시각은 **예시 템플릿(고정 시각 단정 금지)**, 실제 하루 실사례는 트립 후.
- `src/4-workflow/data-power.md` — `# 데이터·전원 운용`. 다룰 내용: 메모리카드 운용·현장 백업 흐름, 멀티데이 전원(파워뱅크·차량 충전·인버터). 정직성: 카드/파워뱅크 용량·요금은 검증/미확인, 특정 SSD·인버터·클라우드는 추천만(정밀 스펙·요금 단정 금지).
- `src/4-workflow/after-return.md` — `# 귀국 후 정리`. 다룰 내용: 3-2-1 백업 전략, 선별(컬링)·편집 순서 허브(트랙별 편집은 기존 파트로 라우팅). 정직성: 특정 백업 장비·클라우드 요금 단정 금지(추천만). 승계 예: 편집은 [여행 사진 보정](../1-travel/3-editing/index.md) / [천체사진 보정](../3-astro/5-postprocessing/index.md) / [CapCut 영상 편집](../2-drone/4-capcut/index.md).

**주의:** 모든 링크 대상은 실제 존재해야 한다(위 승계 링크의 기존 파트 경로는 SUMMARY.md에 실재; 4부 내부 링크는 이 태스크에서 함께 생성되는 5개 파일). 아직 없는 앵커로 링크하지 말 것.
  </action>
  <verify>
`ls src/4-workflow/` 로 5개 파일 존재 확인. `grep -c "작성 예정" src/4-workflow/preparation.md src/4-workflow/field-day.md src/4-workflow/data-power.md src/4-workflow/after-return.md` 각 ≥1. `grep -l "촬영·편집법\|재설명\|승계\|1-travel/index.md" src/4-workflow/index.md` 매치. index.md 는 30줄 이상(`wc -l`).
  </verify>
  <done>src/4-workflow/에 5개 .md 존재. index.md는 흐름 안내 + 4개 트랙 링크 + 1·2·3부 승계 선언 + 정직성 노트를 담은 실 산문이다. 4개 스텁은 각각 H1 + 다룰 내용 1~2문장 + '작성 예정' 노트 + 트랙별 정직성 단서를 담는다. 모든 내부 링크는 실재 파일을 가리킨다.</done>
</task>

<task type="auto">
  <name>Task 2: 목차 배선 — SUMMARY.md 4부 섹션 + introduction.md 카드 그룹 + README 4부 항목</name>
  <files>
src/SUMMARY.md
src/introduction.md
README.md
  </files>
  <action>
**(1) `src/SUMMARY.md`** — `3-astro/7-references/faq.md` 줄(3부의 마지막 항목)과 `# 부록` 사이에 새 섹션을 삽입한다. 정확히 다음 블록(빈 줄 포함):

```
# 4부 · 현장 워크플로

- [현장 워크플로 개요](4-workflow/index.md)
- [여행 전 준비](4-workflow/preparation.md)
- [하루 현장 운용](4-workflow/field-day.md)
- [데이터·전원 운용](4-workflow/data-power.md)
- [귀국 후 정리](4-workflow/after-return.md)

```

삽입 후 `# 부록`이 이 블록 바로 뒤에 오는지 확인. 5개 링크 ↔ Task 1의 5개 파일과 1:1.

**(2) `src/introduction.md`** — 카드 목차(`<nav class="book-toc">`) 안에서 **부록 `<div class="toc-part">` 그룹 바로 앞**에 4부 그룹을 삽입한다(3부 그룹 뒤·부록 그룹 앞). 기존 카드 컨벤션을 그대로 따르며, 개요 카드는 다른 파트처럼 evocative 제목("여행을 완성하다")을 쓴다. 정확히:

```html
<div class="toc-part">
<a class="toc-part-title" href="4-workflow/index.html">4부 · 현장 워크플로</a>
<div class="toc-grid">
<a class="toc-card" href="4-workflow/index.html"><span class="t">여행을 완성하다</span><span class="d">4부 개요 · 준비·하루·정리</span></a>
<a class="toc-card" href="4-workflow/preparation.html"><span class="t">여행 전 준비</span><span class="d">통합 패킹 · 촬영 계획 · 오프라인 앱</span></a>
<a class="toc-card" href="4-workflow/field-day.html"><span class="t">하루 현장 운용</span><span class="d">하루 타임라인 · 세 카메라 오케스트레이션</span></a>
<a class="toc-card" href="4-workflow/data-power.html"><span class="t">데이터·전원 운용</span><span class="d">카드·현장 백업 · 멀티데이 전원</span></a>
<a class="toc-card" href="4-workflow/after-return.html"><span class="t">귀국 후 정리</span><span class="d">3-2-1 백업 · 선별·편집 순서</span></a>
</div>
</div>
```

**(3) `README.md`** — 목차 리스트에서 `- **3부 · 천체사진 (은하수)** …` 줄과 `- **부록** …` 줄 사이에 4부 항목 1줄을 삽입:

```
- **4부 · 현장 워크플로** — 세 트랙을 한 여행으로 잇는 가로 연결: 여행 전 준비(통합 패킹·촬영 계획·오프라인 앱) + 하루 현장 운용(하루 타임라인·세 카메라 오케스트레이션) + 데이터·전원 운용(카드·현장 백업·멀티데이 전원) + 귀국 후 정리(3-2-1 백업·선별·편집 순서 허브) — 각 트랙 촬영·편집법은 1·2·3부로 링크 승계
```
  </action>
  <verify>
`grep -n "# 4부 · 현장 워크플로" src/SUMMARY.md` 매치이고 그 뒤 5개 `4-workflow/*.md` 링크 존재, `# 부록`이 뒤따름. `grep -c "4-workflow/index.html" src/introduction.md` ≥1 이고 4부 toc-part가 부록 toc-part 앞에 위치. `grep -n "4부 · 현장 워크플로" README.md` 매치. SUMMARY의 5개 파일명이 `ls src/4-workflow/`와 정확히 일치.
  </verify>
  <done>SUMMARY.md에 3부와 부록 사이 '# 4부 · 현장 워크플로' 섹션(5개 링크)이 있고, introduction.md 카드 목차에 부록 앞 4부 그룹(개요 evocative 카드 포함)이 있으며, README.md 목차에 4부 항목이 3부와 부록 사이에 있다. SUMMARY 링크 ↔ 실제 파일 1:1.</done>
</task>

<task type="auto">
  <name>Task 3: 검증 게이트 — mdbook build 그린 + 내부 링크 dead 0 + docs 미커밋 + 커밋</name>
  <files>
docs/ (복원만 — 커밋 금지)
  </files>
  <action>
콘텐츠·배선이 끝난 뒤 실행하는 최종 게이트. `create-missing=false`이므로 SUMMARY↔파일 불일치는 빌드 실패로 잡히고, 본문 상호링크의 죽은 링크는 파이썬 전수 스캔으로 교차 검증한다(v1.4 23-02 방식, false-PASS 방지).

1. **빌드 그린:** `/opt/homebrew/bin/mdbook build src` 실행. 종료코드 0, "not found"/"dead"/broken-link 경고 없이 그린이어야 한다. 실패 시 원인(대개 SUMMARY 링크 오타 또는 누락 파일)을 고치고 재실행.

2. **docs 복원(커밋 방지):** `git restore docs/ 2>/dev/null; git clean -fdq docs/ 2>/dev/null`.

3. **내부 링크 전수 스캔** — `src/**/*.md`의 마크다운 링크 `\]\(([^)]+)\)`를 스캔하되 `http`/`https`/`mailto`/`#`/`<`로 시작하는 대상은 제외하고, 대상 파일이 실제 존재하는지 확인한다. 알려진 pre-existing false-positive 2건은 제외:
   - `src/introduction.md`의 `images/intro/파일명.jpg` (인라인 코드 자리표시자 — 경로에 `파일명` 포함)
   - `src/3-astro/3-practice/template.md`의 `../images/practice/session-N/xxx.jpg` (연습 로그 템플릿 자리표시자 — 경로에 `session-N` 포함)
   스캔 스크립트는 scratchpad에 작성해 실행(저장소에 커밋하지 않음). **REAL DEAD(필터 후)가 0이면 통과.** 0이 아니면 각 DEAD를 나열하고 원인(대개 이번에 추가한 4부 링크)을 고친 뒤 1~3을 반복.

4. **docs 재복원 확인:** `git restore docs/ 2>/dev/null; git clean -fdq docs/ 2>/dev/null`. `git status --porcelain docs/`가 비어 있어야 한다.

5. **개별 스테이징 + 커밋** — `src/**`와 `README.md`만 개별 add(절대 `git add -A`/`docs/` 금지):
   ```
   git add src/4-workflow/index.md src/4-workflow/preparation.md src/4-workflow/field-day.md src/4-workflow/data-power.md src/4-workflow/after-return.md src/SUMMARY.md src/introduction.md README.md
   git status --porcelain   # docs/ 항목이 없어야 함
   git commit -m "$(cat <<'EOF'
docs(24): 4부 · 현장 워크플로 파트 신설 + 개요 랜딩 + 트랙 스텁

- src/4-workflow/ 신설: 개요 랜딩(index) + 준비·하루·데이터전원·귀국후 스텁 4장
- SUMMARY.md에 3부↔부록 사이 '4부 · 현장 워크플로' 섹션(5링크)
- introduction.md 카드 목차에 4부 그룹(부록 앞), README 4부 항목
- mdbook build src 그린(create-missing=false), 내부 링크 dead 0
- Phase 25~28이 각 스텁을 채울 구조 뼈대

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
   ```
  </action>
  <verify>
`/opt/homebrew/bin/mdbook build src` 종료코드 0, 죽은 링크 경고 없음. 파이썬 스캔 REAL DEAD(필터 후)=0(알려진 false-positive 2건만 raw에 허용). `git status --porcelain docs/` 비어 있음. `git log --oneline -1`에 `docs(24)` 커밋. `git show --stat HEAD`에 docs/ 파일 없음, src/4-workflow 5개 + SUMMARY + introduction + README 만 포함.
  </verify>
  <done>mdbook build src가 그린으로 통과하고, 내부 링크 전수 스캔 REAL DEAD=0이며, docs/는 미커밋 상태로 복원되고, 8개 src/README 파일만 담긴 docs(24) 커밋이 생성됐다.</done>
</task>

</tasks>

<verification>
- `/opt/homebrew/bin/mdbook build src` 종료코드 0, 그린(create-missing=false → SUMMARY 죽은 링크·누락 파일도 빌드 실패로 검출).
- 내부 링크 전수 스캔 REAL DEAD=0 (알려진 false-positive 2건: `파일명`, `session-N` 경로만 raw에 허용).
- SUMMARY.md의 4부 5개 링크 ↔ `src/4-workflow/`의 5개 파일 1:1 일치.
- introduction.md 카드 목차 4부 그룹이 부록 그룹 앞에 위치, README 목차에 4부 항목 존재.
- docs/ 미커밋(restore/clean 확인), 커밋에는 src/**·README.md만 포함.
</verification>

<success_criteria>
1. 독자가 좌측 목차와 소개 카드 목차에서 3부와 부록 사이 "4부 · 현장 워크플로" + 개요 랜딩을 본다.
2. 파트의 예정 챕터 4개가 정직한 스텁으로 존재하고 SUMMARY 항목과 1:1 일치(create-missing=false 위반 없음).
3. `mdbook build src` 그린 + 내부 링크 dead 0.
4. 개요 랜딩이 준비 → 하루 → 데이터·전원 → 귀국 후 흐름을 안내하고, 각 트랙 촬영·편집법은 기존 1·2·3부 링크로 승계함을 밝힌다.
</success_criteria>

<output>
완료 후 `.planning/phases/24-part-scaffold-stubs/24-01-SUMMARY.md`를 생성한다. 다음을 기록: 생성한 5개 파일 경로, SUMMARY/introduction/README 삽입 위치, 선택한 구조(옵션 a — 5개 평면 페이지)와 근거, 빌드 그린 결과와 링크 스캔 TOTAL/RAW DEAD/REAL DEAD 수치, docs 미커밋 확인, 커밋 해시. Phase 25~28이 각 스텁을 어떤 요구사항으로 채울지(PREP/FIELD/DATA/POST) 승계 메모.
</output>
