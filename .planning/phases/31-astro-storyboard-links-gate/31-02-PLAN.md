---
phase: 31-astro-storyboard-links-gate
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "책 전체 `mdbook build src`가 그린으로 통과한다(create-missing=false → SUMMARY 죽은 링크·누락 파일도 빌드 실패로 검출), WARN/ERROR 없음"
    - "src/**/*.md 전체(1·2·3·4부 + 스토리보드 + 은하수 스토리보드 배치·15개 명소 역링크 포함)의 내부 마크다운 링크를 전수 스캔해 대상 파일이 모두 존재하며 REAL DEAD = 0이다(알려진 pre-existing false-positive 제외: 경로에 `파일명`·`session-N`)"
    - "LINK-CHECK 리포트가 phase dir에 기록되어 TOTAL·RAW DEAD·REAL DEAD 수치와 스캔 범위·제외 항목·PASS/FAIL 판정을 남긴다"
    - "빌드 산출물 docs/는 커밋되지 않는다(restore/clean 전후, 스테이징 비어 있음)"
    - "리포트 파일만 개별 스테이징으로 커밋된다(git add -A 금지, docs/·src/ 콘텐츠 변경 0)"
  artifacts:
    - path: ".planning/phases/31-astro-storyboard-links-gate/31-02-LINK-CHECK.md"
      provides: "v1.6 마일스톤(영상 스토리보드) 전체 책 빌드 + 내부 링크 무결성 최종 게이트 리포트"
      contains: "REAL DEAD"
  key_links:
    - from: "링크 스캔"
      to: "src/**/*.md"
      via: "책 전체의 모든 내부 마크다운 링크 `](path)` 대상 파일 존재 검증"
      pattern: "src/.*\\.md"
    - from: "최종 게이트"
      to: "31-01 (은하수 스토리보드 배치 + 15개 명소 역링크 확정)"
      via: "wave 2 직렬 — 31-01 콘텐츠가 디스크에 존재한 뒤 실행(false-PASS 레이스 방지)"
      pattern: "depends_on.*31-01"
---

<objective>
v1.6 마일스톤(영상 스토리보드) **최종 게이트이자 프로젝트 v1.6 완결 게이트**. 31-01이 은하수 타임랩스 스토리보드(3부 timelapse.md 섹션)와 고비 5곳 × 1·2·3부 = 15개 명소 역링크를 디스크에 확정한 뒤, **책 전체** 빌드와 내부 링크 무결성을 전수 검증한다. 콘텐츠를 편집하지 않고 검증만 수행하며, 결과를 LINK-CHECK 리포트로 남긴다.

Purpose: v1.2/v1.3에서 겪은 false-PASS 레이스(콘텐츠 작성 전 게이트가 통과 판정) 방지 — 이 게이트는 반드시 **wave 2 직렬**로 31-01 이후 실행. mdbook build만으로는 SUMMARY 밖 본문 상호링크의 죽은 링크(특히 31-01이 추가한 15개 명소→스토리보드 역링크, 은하수 섹션의 3부 파트 승계 링크)를 놓칠 수 있어 파이썬 전수 스캔을 함께 수행한다. **timelapse.md만이 아니라 책 전체를 스캔**한다.
Output: `.planning/phases/31-astro-storyboard-links-gate/31-02-LINK-CHECK.md`(TOTAL·RAW DEAD·REAL DEAD 리포트), 콘텐츠 변경 0, docs/ 미커밋.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 미러 대상 — v1.5 최종 게이트(동일 방식·스캐너·리포트 형식)
@.planning/milestones/v1.5-phases/28-after-return-final-gate/28-02-PLAN.md
@.planning/milestones/v1.5-phases/28-after-return-final-gate/28-02-LINK-CHECK.md

# 이 게이트가 검증하는 대상 (참고 — 편집하지 않음)
@src/SUMMARY.md
</context>

<constraints>
- 이 플랜은 **콘텐츠를 편집하지 않는다**(files_modified: []). 죽은 링크가 발견되면 수정은 게이트 담당 밖 — 리포트에 REAL DEAD 목록을 남기고, 실제 수정은 gap-closure(31-01 재실행 또는 별도 플랜)로 처리한다.
- **docs/는 절대 커밋하지 않는다.** 빌드 전후로 `git restore docs/ 2>/dev/null; git clean -fdq docs/ 2>/dev/null`.
- 커밋은 **리포트 파일만** 개별 스테이징(`git add -A`/`git add .` 금지, docs/ 금지).
- **책 전체를 스캔한다** — `src/**/*.md` 전부(1·2·3·4부 + 스토리보드 6종 + 은하수 섹션 배치된 timelapse.md + 15개 명소 역링크 + SUMMARY·introduction·about 등).
- 알려진 pre-existing false-positive는 REAL DEAD 집계에서 제외(경로 기반 필터):
  1. `src/about.md`의 `images/intro/파일명.jpg` — 인라인 예시의 자리표시자 파일명(placeholder). 필터 키: 경로에 `파일명` 포함.
  2. `src/3-astro/3-practice/template.md`의 `../../images/practice/session-N/xxx.jpg` — 연습 로그 템플릿 자리표시자. 필터 키: 경로에 `session-N` 포함.
  최종 REAL DEAD(필터 후)는 반드시 **0**이어야 게이트 통과.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: 책 전체 mdbook build src 그린 검증 + docs 복원</name>
  <files>(검증 전용 — 콘텐츠 변경 없음)</files>
  <action>
1. 빌드 전 docs/ 청소: `git restore docs/ 2>/dev/null; git clean -fdq docs/ 2>/dev/null`.
2. `/opt/homebrew/bin/mdbook build src` 실행. create-missing=false이므로 SUMMARY의 죽은 링크·누락 파일이 빌드 실패로 잡힌다. **종료코드 0, WARN/ERROR·죽은 링크 경고 없이 그린**이어야 한다.
3. 실패 시: 출력의 죽은 링크/누락 파일/WARN/ERROR 메시지를 그대로 캡처(Task 2 리포트에 기록). 이 플랜은 콘텐츠를 고치지 않으므로, 실패 원인을 리포트에 남기고 게이트 FAIL로 처리한다(후속 gap-closure 필요 신호).
4. 빌드 산출물 복원(커밋 방지): `git restore docs/ 2>/dev/null; git clean -fdq docs/ 2>/dev/null`.
5. 빌드 결과(그린/실패, WARN/ERROR 유무)를 Task 2 리포트에 넘길 수 있도록 메모.
  </action>
  <verify>
- `/opt/homebrew/bin/mdbook build src` 종료코드 0.
- 빌드 출력에 "not found"/"dead"/broken link/WARN/ERROR 관련 경고 없음.
- `git status --porcelain docs/` 비어 있음(docs/ 미스테이징).
  </verify>
  <done>mdbook build src가 그린(WARN/ERROR 없음)으로 통과하고 docs/ 산출물이 복원되어 스테이징에 없다(또는 실패 원인이 캡처되어 게이트 FAIL로 기록됨).</done>
</task>

<task type="auto">
  <name>Task 2: 내부 링크 전수 스캔(REAL DEAD 0) → LINK-CHECK 리포트 작성 + 개별 커밋</name>
  <files>.planning/phases/31-astro-storyboard-links-gate/31-02-LINK-CHECK.md</files>
  <action>
1. 아래 파이썬 스크립트를 스크래치패드에 저장 후 실행(레포 루트에서). 모든 `src/**/*.md`의 내부 마크다운 링크 `](path)`를 추출해 대상 존재를 검증한다. http/https/mailto/tel/#앵커/각괄호 외부 URL은 제외하고, 앵커(#...)는 잘라 각 파일 위치 기준 상대경로로 정규화한다. false-positive는 **경로 부분 문자열**(`파일명`, `session-N`)로 필터한다.

```python
import os, re, sys

SRC = "src"
link_re = re.compile(r'\]\(\s*(<[^>]+>|[^)]+)\)')

# 알려진 pre-existing false-positive — 링크 타깃 경로에 아래 부분 문자열이 있으면 REAL DEAD 집계 제외
#   파일명    -> src/about.md 의 images/intro/파일명.jpg (자리표시자)
#   session-N -> src/3-astro/3-practice/template.md 의 .../session-N/xxx.jpg (템플릿 자리표시자)
FALSE_POS_SUBSTR = ("파일명", "session-N")

total = 0
raw_dead = []   # 모든 미존재 타깃 (rel, target, resolved)
for root, _, files in os.walk(SRC):
    for fn in sorted(files):
        if not fn.endswith(".md"):
            continue
        path = os.path.join(root, fn)
        rel = os.path.relpath(path)
        with open(path, encoding="utf-8") as f:
            text = f.read()
        for m in link_re.finditer(text):
            target = m.group(1).strip()
            if target.startswith("<") and target.endswith(">"):
                target = target[1:-1].strip()
            if target.startswith(("http://", "https://", "mailto:", "tel:", "#")):
                continue
            noanchor = target.split("#", 1)[0].strip()
            if not noanchor:
                continue
            total += 1
            resolved = os.path.normpath(os.path.join(os.path.dirname(path), noanchor))
            if not os.path.exists(resolved):
                raw_dead.append((rel, target, resolved))

def is_fp(target):
    return any(s in target for s in FALSE_POS_SUBSTR)

real_dead = [d for d in raw_dead if not is_fp(d[1])]

print("TOTAL internal links:", total)
print("RAW DEAD (incl. known false-positives):", len(raw_dead))
for d in raw_dead:
    tag = " [known-FP]" if is_fp(d[1]) else ""
    print(f"  {d[0]} -> {d[1]}  ({d[2]}){tag}")
print("REAL DEAD (filtered):", len(real_dead))
sys.exit(1 if real_dead else 0)
```

2. 스크립트 출력을 캡처. **REAL DEAD가 0이면 게이트 통과.** 0이 아니면 각 REAL DEAD를 리포트에 나열하고 게이트 FAIL로 기록(후속 gap-closure 신호). raw_dead에 알려진 2건 외 항목이 있으면 반드시 조사(특히 31-01이 추가한 은하수 섹션의 3부 파트 승계 링크·15개 명소→스토리보드 역링크).
3. 필요 시 `mdbook build src`를 한 번 더 돌려 SUMMARY 측 죽은 링크 없음을 교차 확인(create-missing=false) 후 docs/ 재청소.
4. `.planning/phases/31-astro-storyboard-links-gate/31-02-LINK-CHECK.md` 리포트 작성(v1.5 28-02-LINK-CHECK.md 형식 미러). 포함 항목:
   - 실행 일시, 게이트 설명(v1.6 최종, wave 2 직렬), 콘텐츠 변경 없음.
   - `mdbook build src` 결과(그린/실패, WARN/ERROR 유무).
   - 스캔 범위(`src/**/*.md` — 모든 파트 + 스토리보드 + 은하수 섹션 + 15개 명소 역링크), 스캔 규칙(제외: http/https/mailto/tel/#앵커/각괄호 외부 URL; 앵커 스트립; 파일별 상대경로 정규화).
   - **TOTAL internal links, RAW DEAD, REAL DEAD** 표.
   - 알려진 false-positive 2건 명시(src/about.md `images/intro/파일명.jpg`, src/3-astro/3-practice/template.md `.../session-N/xxx.jpg`)와 제외 근거(경로 부분 문자열 `파일명`·`session-N`).
   - 최종 판정: **PASS**(build 그린 + REAL DEAD = 0) 또는 **FAIL**(원인·REAL DEAD 목록).
   - v1.6 마일스톤 최종 상태(Phase 29~31) 및 요건 커버(SB-01·SB-02·SB-03·ASSET-01·ASSET-02·LINK-01) 요약.
5. docs/ 복원 재확인: `git restore docs/ 2>/dev/null; git clean -fdq docs/ 2>/dev/null`.
6. **리포트만 개별 스테이징 커밋**(`git add -A`/`git add .` 금지, docs/ 금지):
   `git add .planning/phases/31-astro-storyboard-links-gate/31-02-LINK-CHECK.md`
   `git commit` 메시지:
   ```
   docs(31-02): v1.6 최종 게이트 — 전체 빌드 그린 + 내부 링크 dead 0

   - mdbook build src 그린(create-missing=false, WARN/ERROR 없음)
   - src/**/*.md 전체 내부 링크 전수 스캔: TOTAL <n>, REAL DEAD 0(known FP 2건 제외)
   - LINK-CHECK 리포트 기록, docs/ 미커밋 — v1.6 영상 스토리보드 완결

   Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
   ```
   (FAIL이면 메시지를 실제 결과에 맞게 조정하고 REAL DEAD 요약 기재.)
7. `git show --stat HEAD`로 리포트 파일만 커밋되고 docs/·src/ 콘텐츠 변경이 없음을 확인.
  </action>
  <verify>
- 스크립트 출력의 `REAL DEAD (filtered): 0`.
- `mdbook build src` 종료코드 0.
- `.planning/phases/31-astro-storyboard-links-gate/31-02-LINK-CHECK.md` 존재하고 TOTAL·RAW DEAD·REAL DEAD·PASS/FAIL 판정 포함.
- `git show --stat HEAD`에 리포트 파일만 포함, docs/ 및 src/ 콘텐츠 변경 없음.
- `git status --porcelain docs/` 비어 있음.
  </verify>
  <done>책 전체 내부 링크 전수 스캔 REAL DEAD=0, mdbook build src 그린, LINK-CHECK 리포트가 phase dir에 기록되고 리포트만 개별 스테이징으로 커밋되었으며 docs/는 미커밋이다.</done>
</task>

</tasks>

<verification>
- `mdbook build src` 그린(create-missing=false로 SUMMARY 죽은 링크도 검출, WARN/ERROR 없음).
- 파이썬 전수 스캔 REAL DEAD=0, 알려진 false-positive 2건만 raw_dead에 허용(경로에 `파일명`·`session-N`).
- 스캔 범위가 책 전체(`src/**/*.md`, 모든 파트 + 스토리보드 + 은하수 섹션 + 15개 명소 역링크).
- LINK-CHECK 리포트에 스캔 범위·규칙·TOTAL·RAW DEAD·REAL DEAD·판정 기록.
- 콘텐츠 변경 0, docs/ 미커밋, 리포트만 개별 커밋.
</verification>

<success_criteria>
- 게이트: `mdbook build src` 그린 + 책 전체 내부 링크 전수 스캔 REAL DEAD 0(known FP 제외).
- wave 2 직렬(depends_on ["31-01"])로 31-01 콘텐츠 확정 이후 실행 — false-PASS 레이스 방지.
- LINK-CHECK 리포트가 phase dir에 존재하고 최종 PASS 판정, docs/ 미커밋. v1.6 영상 스토리보드 완결.
</success_criteria>

<output>
완료 후 `.planning/phases/31-astro-storyboard-links-gate/31-02-SUMMARY.md` 생성(템플릿 형식). 게이트 결과(TOTAL·REAL DEAD·PASS)와 v1.6 마일스톤 최종 상태(Phase 29~31·요건 6/6)를 frontmatter/본문에 기록.
</output>
