---
phase: 02-연습-1차-집-2차-서해안
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/03-practice/session-1-home.md
  - src/images/practice/session-1/.gitkeep
  - src/SUMMARY.md
autonomous: false   # Task 2 is checkpoint:human-action — user must run a real home session and report data

must_haves:
  truths:
    - "독자가 연습 1차(집) 챕터를 열면 template.md와 동일한 6개 섹션(목표/장비·세팅/촬영 환경/결과 사진/배운 점·문제점/다음 세션 개선 사항)을 본다"
    - "세션 전에도 '목표' 섹션에 구체적 홈 리허설 드릴이 적혀 있다 (M모드 노출 3요소, 500룰 자가계산 지시, 먼 불빛으로 수동 초점 연습, RAW+수동 WB, 인터벌/리모컨)"
    - "세션 전 나머지 5개 섹션은 '(세션 후 채움)' 플레이스홀더로 남아 있고 촬영 결과·세팅·배운 점이 날조되지 않는다"
    - "세션 후 실제 사용자 데이터(장비·세팅값/환경/배운 점)가 그대로 5개 섹션에 반영된다"
    - "실제 촬영 사진이 optimize-images.sh로 리사이즈·EXIF 제거되어 src/images/practice/session-1/에 수록된다"
    - "SUMMARY 사이드바의 '연습 1차 — 집' 링크가 실제 페이지로 연결되어 도달 가능하다"
    - "'다음 세션 개선 사항' 섹션이 채워져 연습 2차의 '목표'로 이어질 내용이 존재한다"
  artifacts:
    - path: "src/03-practice/session-1-home.md"
      provides: "PRAC-02 연습 1차(집) 로그 — template의 6개 섹션 구조 재사용"
      contains: "다음 세션 개선"
      min_lines: 25
    - path: "src/images/practice/session-1/.gitkeep"
      provides: "세션 1 사진 폴더 (ingest 대상, Phase 2가 생성)"
    - path: "src/SUMMARY.md"
      provides: "연습 1차 draft 링크를 실제 경로로 flip"
      contains: "03-practice/session-1-home.md"
  key_links:
    - from: "src/03-practice/session-1-home.md"
      to: "src/03-practice/template.md (6개 섹션 구조)"
      via: "template의 fenced 블록을 그대로 복사한 6개 섹션 헤더"
      pattern: "## 목표|## 장비|## 촬영 환경|## 결과 사진|## 배운 점|## 다음 세션 개선"
    - from: "src/SUMMARY.md"
      to: "src/03-practice/session-1-home.md"
      via: "draft () → 실제 상대경로 링크 flip"
      pattern: "연습 1차 — 집\\]\\(03-practice/session-1-home.md\\)"
    - from: "scripts/optimize-images.sh"
      to: "src/images/practice/session-1/"
      via: "사용자 원본 사진 폴더를 resize+EXIF strip 처리해 ingest"
      pattern: "optimize-images.sh"
---

<objective>
연습 1차(집) 로그 챕터를 template.md와 동일한 6개 섹션 구조로 만들고, 세션 전에 작성 가능한 '목표'(홈 리허설 드릴)를 채운 뒤, 실제 세션 후 사용자가 보고한 사진·세팅·배운 점을 나머지 5개 섹션에 반영한다.

Purpose: PRAC-02 (1차 집 연습 기록 — 기본 장비·수동 설정 익히기)를 충족. Phase 성공 기준 #1(연습 1차 챕터에 템플릿 형식대로 6개 섹션이 채워져 있다)을 만족하고, 실전(서해안) 전 마지막 홈 리허설 결과를 book에 기록한다.
Output: src/03-practice/session-1-home.md (신규), src/images/practice/session-1/ 폴더 + 실제 사진, SUMMARY 링크 flip.

핵심 제약(날조 금지): 목표를 제외한 5개 섹션(장비·세팅/환경/결과 사진/배운 점/다음 개선)은 실제 세션이 끝나기 전에는 채우지 않는다 — template.md의 house rule("실제 세션이 끝나기 전에는 이미지 파일을 만들지 않는다")을 그대로 따른다. Task 2 checkpoint가 실제 데이터를 공급하기 전까지는 '(세션 후 채움)' 플레이스홀더로 둔다.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-연습-1차-집-2차-서해안/02-RESEARCH.md
@src/03-practice/template.md
@src/02-fundamentals/focusing.md
@src/02-fundamentals/500-npf-rule.md
@src/02-fundamentals/field-workflow.md
@src/02-fundamentals/finding-the-milkyway.md
@scripts/optimize-images.sh
@src/SUMMARY.md

인용하되 재도출 금지: 6개 섹션 구조·복사 규칙 = template.md 그대로. 초점 연습 절차(먼 가로등/간판 불빛, 라이브뷰 10배, 테이프 고정) = focusing.md §실전 팁("반드시 실제 밤하늘 아래서 처음 시도하지 마세요"), 재작성 말고 링크. 500룰 값은 하드코딩 금지 — 사용자 카메라 마운트가 미확정(STATE.md Blocker)이므로 "본인 카메라·렌즈 크롭계수를 500-npf-rule.md에서 확인해 최대 셔터속도를 직접 계산해 적어두기"라는 지시로 작성한다(RESEARCH.md Pitfall 2). 이미지 최적화는 optimize-images.sh 그대로 사용(재작성 금지). SUMMARY는 '연습 1차 — 집' 한 줄만 flip — 연습 3차/4차 draft는 건드리지 않는다(Phase 3 소관).
</context>

<tasks>

<task type="auto">
  <name>Task 1: 연습 1차 챕터 스캐폴드 + 세션 전 '목표'(홈 리허설 드릴) 작성</name>
  <files>src/03-practice/session-1-home.md, src/images/practice/session-1/.gitkeep</files>
  <action>
1. `src/03-practice/session-1-home.md` 신규 생성. template.md의 fenced 블록(6개 섹션)을 **구조 그대로** 복사해 실제 마크다운으로 옮긴다. H1은 `# 연습 1차 — 집 (2026-MM-DD)` (날짜는 세션 후 실제 날짜로 확정 — 지금은 `MM-DD` 자리표시 유지).

2. '## 목표' 섹션만 지금 구체적으로 채운다(세션 전 작성 가능한 유일한 섹션). 홈 리허설 드릴을 항목 리스트로:
   - M모드로 전환해 노출 3요소(ISO/조리개/셔터)를 직접 조작해본다 → [노출의 기본](../02-fundamentals/exposure-basics.md) 참조.
   - 본인 카메라·렌즈의 크롭계수를 [500 / NPF 룰](../02-fundamentals/500-npf-rule.md)에서 확인해 최대 셔터속도를 **직접 계산**해 목표값으로 적어둔다 (하드코딩된 숫자 쓰지 말 것 — 마운트/렌즈 확정 전이므로 자가계산 지시로).
   - 별이 아닌 **먼 가로등·간판 불빛**을 대상으로 라이브뷰 10배 확대 → 수동 초점 → 테이프 고정까지 전체 절차를 미리 연습한다 → [별에 초점 맞추기](../02-fundamentals/focusing.md)의 방법을 그대로 따른다(재작성 말고 링크; focusing.md가 "밤하늘 아래서 처음 시도하지 말고 집에서 미리 연습" 을 명시).
   - RAW 저장 + 수동 화이트밸런스 고정(3800~4200K) 설정을 확인한다 → [RAW와 화이트밸런스](../02-fundamentals/raw-and-wb.md).
   - 인터벌 타이머/리모컨으로 셔터를 손으로 누르지 않고 촬영하는 법을 확인한다.
   - (선택) 삼각대 설치·수평, 시험 촬영 후 100% 확대해 불빛이 점으로 맺히는지 확인하는 습관.
   맨 위에 한 문장: 이 세션은 은하수를 찍는 연습이 아니라 세팅·초점·워크플로를 몸에 익히는 홈 리허설이라는 프레이밍([현장 촬영 워크플로](../02-fundamentals/field-workflow.md) 순서를 집에서 예행연습).

3. 나머지 5개 섹션('장비 & 세팅' / '촬영 환경' / '결과 사진' / '배운 점 / 문제점' / '다음 세션 개선 사항')은 본문을 명시적 플레이스홀더로 둔다: `(세션 후 채움 — ...)` 형태. 예: 장비 & 세팅 → `(세션 후 채움 — 실제 사용 장비/세팅값)`, 결과 사진 → `(세션 후 채움 — 실제 촬영 사진, session-1/ 폴더)`, 다음 세션 개선 사항 → `(세션 후 채움 — 이 항목이 연습 2차 "목표"로 이어짐)`. **실제 이미지 파일이나 세팅 숫자를 지어내지 않는다.**

4. 이미지 폴더 생성: `mkdir -p src/images/practice/session-1 && touch src/images/practice/session-1/.gitkeep` (practice/ 상위 폴더도 이때 함께 생성됨 — Phase 2가 자기 폴더를 만드는 것이 규약).
  </action>
  <verify>`mdbook build src` exits 0; session-1-home.md에 6개 섹션 헤더(## 목표/## 장비 & 세팅/## 촬영 환경/## 결과 사진/## 배운 점/## 다음 세션 개선) 모두 존재; '목표'에 M모드·500룰 자가계산 지시·초점 링크·RAW/WB·인터벌 5개 드릴 존재; 하드코딩된 500룰 숫자 없음; 나머지 5개 섹션에 '(세션 후 채움' 플레이스홀더 존재; src/images/practice/session-1/.gitkeep 존재.</verify>
  <done>연습 1차 챕터가 template과 동일한 6개 섹션 구조로 존재하고, 세션 전 목표(드릴)가 채워졌으며, 나머지 5개 섹션은 날조 없이 플레이스홀더 상태다.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>연습 1차 챕터(session-1-home.md)에 6개 섹션 구조 + 세션 전 '목표'(홈 리허설 드릴)가 작성됨. 나머지 5개 섹션은 '(세션 후 채움)' 상태. 사진 폴더 src/images/practice/session-1/ 준비됨.</what-built>
  <action>연습 1차(집) 세션을 실제로 진행하고 결과를 알려주세요.</action>
  <how-to-verify>
위 '목표' 섹션의 드릴대로 집(발코니/창밖 등)에서 실제로 촬영·연습을 진행한 뒤, 아래를 텍스트로 알려주세요. Claude가 이 정보를 나머지 5개 섹션에 그대로 반영합니다(내용을 지어내지 않고, 알려주신 값만 사용).
1. 사진 파일이 있는 폴더 경로 (예: ~/Desktop/session1-raw/) — Claude가 optimize-images.sh로 resize+EXIF strip 처리해 src/images/practice/session-1/에 넣습니다. (없으면 "사진 없음"이라고 알려주세요.)
2. 실제 사용한 장비 + 세팅값: 카메라/렌즈, 조리개 / ISO / 셔터스피드 / 초점거리 / 화이트밸런스.
3. 촬영 환경: 날짜, 시간대, 날씨, 위치 설명(집 발코니 등), (해당 시) 달·광해.
4. 잘 된 것 / 실패한 것 + 원인 (한 줄씩이라도).
5. 다음 세션(2차 서해안)에서 바꾸거나 개선하고 싶은 것 — 이 항목이 연습 2차의 '목표'가 됩니다.
  </how-to-verify>
  <resume-signal>사진 폴더 경로(또는 "사진 없음") + 2~5번 답변을 텍스트로 제공하면 Claude가 Task 3를 진행합니다.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 실제 데이터 반영(사진 ingest + 5개 섹션 채움) + SUMMARY 링크 flip</name>
  <files>src/03-practice/session-1-home.md, src/images/practice/session-1/, src/SUMMARY.md</files>
  <action>
Task 2 checkpoint가 resume된 뒤에만 실행. 사용자가 준 값만 사용하고 어떤 것도 추정/날조하지 않는다.

1. 사진이 있으면: `bash scripts/optimize-images.sh <사용자-제공-경로> src/images/practice/session-1/` 실행(사전에 `magick`/ImageMagick 설치 확인). 결과 파일명을 확인해 '결과 사진' 섹션에 `![설명](../images/practice/session-1/파일명.jpg)` + 캡션으로 삽입. 사진이 없으면 '결과 사진'에 "이번 세션은 사진 없음(세팅·초점 연습 위주)" 등 사용자가 알려준 사실만 기록.

2. H1의 날짜 `MM-DD`를 사용자가 알려준 실제 날짜로 확정.

3. 나머지 섹션을 사용자 보고 그대로 채움:
   - 장비 & 세팅: 2번 답변(카메라/렌즈 + 조리개/ISO/셔터/초점거리/WB).
   - 촬영 환경: 3번 답변(날짜/시간/날씨/위치).
   - 배운 점 / 문제점: 4번 답변(잘 된 것/실패한 것/원인).
   - 다음 세션 개선 사항: 5번 답변 — **연습 2차의 '목표'로 그대로 이어지도록** 명확한 개선 항목 형태로 정리(cross-session chain의 출발점).
   모든 '(세션 후 채움)' 플레이스홀더가 제거되었는지 확인.

4. `src/SUMMARY.md`에서 `- [연습 1차 — 집]()` 한 줄만 `- [연습 1차 — 집](03-practice/session-1-home.md)`로 flip. 연습 2차/3차/4차 draft 줄은 절대 건드리지 않는다.
  </action>
  <verify>`mdbook build src` exits 0; session-1-home.md에 '(세션 후 채움)' 문자열이 하나도 남아있지 않음; '다음 세션 개선 사항'에 실제 개선 항목 존재; 사진이 있었다면 src/images/practice/session-1/에 최적화된 .jpg 존재 + '결과 사진'에 이미지 링크; `grep '03-practice/session-1-home.md' src/SUMMARY.md` 매치; SUMMARY의 연습 3차/4차 줄은 여전히 draft `()`.</verify>
  <done>연습 1차 챕터 6개 섹션이 실제 사용자 데이터로 모두 채워지고, 사진이 EXIF 제거된 채 수록되며, 사이드바 링크로 도달 가능하고, '다음 세션 개선 사항'이 2차 목표로 이어질 상태다.</done>
</task>

</tasks>

<verification>
- `mdbook build src` exits 0; session-1-home.md 렌더링, 사이드바에서 도달 가능.
- 6개 섹션 = template.md 구조와 동일(comparability 유지); 목표 외 섹션은 실제 데이터로만 채워짐(날조 없음).
- 사진은 optimize-images.sh를 거쳐 EXIF/GPS 제거된 상태로 session-1/에 수록.
- PRAC-02 커버: 기본 장비·수동 설정을 익히는 홈 세션이 목표·결과로 기록됨.
</verification>

<success_criteria>
Phase 성공 기준 #1(연습 1차 챕터에 템플릿 형식대로 목표/장비·세팅/환경/결과 사진/배운 점/다음 개선이 채워져 있다) 충족. '다음 세션 개선 사항'이 연습 2차 '목표'의 입력으로 준비됨.
</success_criteria>

<output>
After completion, create `.planning/phases/02-연습-1차-집-2차-서해안/02-01-SUMMARY.md`.
</output>
