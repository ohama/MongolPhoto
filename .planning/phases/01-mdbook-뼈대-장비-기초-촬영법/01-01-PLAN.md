---
phase: 01-mdbook-뼈대-장비-기초-촬영법
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/book.toml
  - src/SUMMARY.md
  - src/introduction.md
  - src/theme/custom.css
  - src/theme/fonts/fonts.css
  - src/theme/fonts/NotoSansKR-Regular.woff2
  - src/theme/fonts/NotoSansKR-Bold.woff2
  - src/01-gear/index.md
  - src/01-gear/camera-lens.md
  - src/01-gear/accessories.md
  - src/01-gear/checklist.md
  - src/02-fundamentals/index.md
  - src/02-fundamentals/exposure-basics.md
  - src/02-fundamentals/500-npf-rule.md
  - src/02-fundamentals/focusing.md
  - src/02-fundamentals/raw-and-wb.md
  - src/02-fundamentals/finding-the-milkyway.md
  - src/02-fundamentals/field-workflow.md
  - src/03-practice/template.md
  - src/images/gear/.gitkeep
  - src/images/fundamentals/.gitkeep
  - scripts/optimize-images.sh

must_haves:
  truths:
    - "`mdbook build src` succeeds with zero errors (all SUMMARY.md links resolve)"
    - "`mdbook serve src` renders Korean text in Noto Sans KR with word-break: keep-all applied"
    - "SUMMARY.md shows the entire planned 6-part book: Phase 1 chapters as real links, all later-phase entries as greyed-out draft links"
    - "Every Phase 1 chapter file exists as a stub reachable from the sidebar (no 404, no missing-file build error)"
    - "scripts/optimize-images.sh resizes + strips EXIF/GPS in one command and writes into src/images/<subfolder>/"
  artifacts:
    - path: "src/book.toml"
      provides: "mdBook config: language=ko, src='.', build-dir='../docs', additional-css theme/custom.css, git-repository-url + edit-url on main branch"
      contains: "language = \"ko\""
    - path: "src/theme/custom.css"
      provides: "Korean font stack + word-break:keep-all"
      contains: "word-break: keep-all"
    - path: "src/theme/fonts/fonts.css"
      provides: "@font-face for Noto Sans KR 400/700, local relative woff2 paths"
      contains: "@font-face"
    - path: "src/SUMMARY.md"
      provides: "Full 6-part skeleton with draft links for future phases"
      contains: "연습 로그 템플릿"
    - path: "src/03-practice/template.md"
      provides: "Reader-visible practice-log template stub (filled by plan 04)"
      min_lines: 1
    - path: "scripts/optimize-images.sh"
      provides: "ImageMagick resize + -strip + quality wrapper"
      contains: "-strip"
  key_links:
    - from: "src/book.toml"
      to: "src/theme/custom.css"
      via: "additional-css array"
      pattern: "additional-css.*custom.css"
    - from: "src/theme/custom.css"
      to: "src/theme/fonts/fonts.css"
      via: "@import"
      pattern: "@import.*fonts.css"
    - from: "src/SUMMARY.md"
      to: "src/01-gear/index.md and all Phase 1 chapter stubs"
      via: "markdown links"
      pattern: "01-gear/index.md"
---

<objective>
Stand up the mdBook skeleton so every later Phase 1 content chapter has a place to live, renders correctly in Korean, and the whole book builds green from the first commit.

Purpose: This is the blocking foundation — Success Criteria #1 (Korean build/preview) and #2 (image pipeline + folder structure) depend on it, and all three content plans (02, 03, 04) edit stub files this plan creates.
Output: A buildable `src/` mdBook project with Korean fonts, a full 6-part SUMMARY.md skeleton (Phase 1 links real, later phases draft), stub chapter files for every Phase 1 page, image folders, and a reusable image-optimization script.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mdbook-뼈대-장비-기초-촬영법/01-RESEARCH.md
@.claude/commands/pages.md
@.claude/commands/mdbook.md
@.claude/skills/mdbook-docs-images.skill.md
@.claude/skills/markdown-image-insertion.skill.md

Environment facts (verified in 01-RESEARCH.md): mdbook v0.5.3 installed; ImageMagick NOT installed (run `brew install imagemagick`); git branch is `main`; origin is https://github.com/ohama/MongolPhoto.git. Do NOT downgrade mdbook. Do NOT add custom preprocessors.
</context>

<tasks>

<task type="auto">
  <name>Task 1: mdBook config + Korean rendering (book.toml, fonts, CSS, intro)</name>
  <files>src/book.toml, src/introduction.md, src/theme/custom.css, src/theme/fonts/fonts.css, src/theme/fonts/NotoSansKR-Regular.woff2, src/theme/fonts/NotoSansKR-Bold.woff2</files>
  <action>
Follow the `/pages` skill's structural pattern (src dir = mdBook project, src=".", build-dir="../docs") but write the config directly with the researched values so execution stays non-interactive.

1. Create `src/book.toml` verbatim per 01-RESEARCH.md "book.toml (target shape)":
   - [book] title="몽골 은하수 촬영기", authors=["ohama"], language="ko", description="트래킹 장비 없이 몽골에서 은하수 사진 찍기 — 장비부터 촬영, 후보정까지", src="."
   - [build] build-dir="../docs", create-missing=false
   - [output.html] default-theme="light", preferred-dark-theme="navy", git-repository-url="https://github.com/ohama/MongolPhoto", edit-url-template="https://github.com/ohama/MongolPhoto/edit/main/src/{path}" (note: main, NOT master), additional-css=["theme/custom.css"]
   - [output.html.fold] enable=true
   - [output.html.search] enable=true, limit-results=30, boost-title=2, boost-hierarchy=1

2. Create `src/theme/custom.css` per 01-RESEARCH.md: `@import url("fonts/fonts.css");` then body { font-family: "Noto Sans KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif; word-break: keep-all; overflow-wrap: break-word; }

3. Self-host Noto Sans KR (400 + 700) into src/theme/fonts/ using the curl recipe in 01-RESEARCH.md "theme/fonts/fonts.css — self-hosting recipe" (modern-browser User-Agent so Google serves woff2). Save the two woff2 files as NotoSansKR-Regular.woff2 / NotoSansKR-Bold.woff2 and write src/theme/fonts/fonts.css with two @font-face blocks (weight 400 + 700) whose src: url() points to the LOCAL relative filenames. If network download fails in this environment, still create fonts.css with the @font-face rules and leave a HTML-comment note that the two woff2 files must be dropped in — the CSS system-font fallback (Apple SD Gothic Neo / Malgun Gothic) keeps Korean rendering regardless, so do not block on the download.

4. Create `src/introduction.md`: an H1 "몽골 은하수 촬영기" + 2-3 sentence 소개 (project intent from PROJECT.md: 트래킹 장비 없이 몽골 은하수 촬영, 초보자가 따라 하는 여정), and a "시작하기" line linking to 01-gear/index.md. Korean prose.
  </action>
  <verify>`mdbook build src` exits 0; grep confirms `word-break: keep-all` in custom.css, `@import` of fonts.css in custom.css, `language = "ko"` in book.toml.</verify>
  <done>book.toml, custom.css, fonts.css, introduction.md exist; build succeeds; Korean font stack + keep-all wired.</done>
</task>

<task type="auto">
  <name>Task 2: Full SUMMARY.md skeleton + stub chapter tree + image folders</name>
  <files>src/SUMMARY.md, src/01-gear/index.md, src/01-gear/camera-lens.md, src/01-gear/accessories.md, src/01-gear/checklist.md, src/02-fundamentals/index.md, src/02-fundamentals/exposure-basics.md, src/02-fundamentals/500-npf-rule.md, src/02-fundamentals/focusing.md, src/02-fundamentals/raw-and-wb.md, src/02-fundamentals/finding-the-milkyway.md, src/02-fundamentals/field-workflow.md, src/03-practice/template.md, src/images/gear/.gitkeep, src/images/fundamentals/.gitkeep</files>
  <action>
1. Create `src/SUMMARY.md` EXACTLY as the skeleton in 01-RESEARCH.md "Pattern 1: SUMMARY.md" — the full 6-part book:
   - [소개](introduction.md) prefix
   - "# 1부 · 준비하기" with REAL links: 01-gear/index.md + camera-lens.md + accessories.md + checklist.md; 02-fundamentals/index.md + exposure-basics.md + 500-npf-rule.md + focusing.md + raw-and-wb.md + finding-the-milkyway.md + field-workflow.md; 03-practice/template.md
   - "# 2부 · 연습 로그" — four DRAFT links `- [연습 1차 — 집]()` … (empty parens = greyed draft, no file)
   - "# 3부 · 몽골 (8/13 ~ 8/17)" — draft entries 여행 개요 + Day 1..Day 5
   - "# 4부 · 후보정" — draft 워크플로 개요
   - "# 부록" — draft 현장 치트시트 / 체크리스트 모음 / 앱·소프트웨어 가이드 / 용어 사전
   Draft links MUST use empty `()` so no file is required and no build error occurs.

2. Create each Phase 1 chapter file as a minimal STUB so build stays green and content plans (02/03/04) fill them in: one H1 matching its SUMMARY title + a single line `> (작성 예정 — 01-02/03/04 플랜에서 채움)`. Files: all four 01-gear/*.md, all seven 02-fundamentals/*.md, and 03-practice/template.md. Do NOT write real content here — that is the content plans' job. Each file owned by exactly one downstream plan.

3. Create image folders per PUB-03 chapter-matched structure: `src/images/gear/.gitkeep` and `src/images/fundamentals/.gitkeep` (empty placeholder files so the folders are tracked). Do NOT create speculative folders for later phases (2부/3부/4부/부록) — those phases create their own.

Follow mdbook-docs-images / markdown-image-insertion house rules for all files (relative image paths under images/, no <img> tags, no raw HTML admonitions, H1-once-per-file).
  </action>
  <verify>`mdbook build src` exits 0 with no "file not found" for any SUMMARY entry; `ls src/01-gear src/02-fundamentals src/03-practice` shows all stub files; draft entries render greyed in the sidebar (spot-check built docs/ HTML or `mdbook serve`).</verify>
  <done>SUMMARY.md lists the full 6-part book; every Phase 1 stub exists and builds; later phases are draft links only; image folders exist.</done>
</task>

<task type="auto">
  <name>Task 3: Image-optimization script + build smoke test (PUB-03)</name>
  <files>scripts/optimize-images.sh</files>
  <action>
1. Ensure ImageMagick is available: `which magick || brew install imagemagick`.

2. Create `scripts/optimize-images.sh` verbatim from 01-RESEARCH.md "Image pipeline script (PUB-03)": takes <input-dir> <output-dir>, loops jpg/jpeg/png, runs `magick "$f" -resize '2000x2000>' -strip -quality 82 "$OUT/${base%.*}.jpg"`, `set -euo pipefail`, `shopt -s nullglob nocaseglob`, prints a done count. Rationale to preserve as a top comment: `-strip` removes EXIF/GPS so trip-photo locations do not leak on the public Pages site (sips cannot do this reliably). `chmod +x scripts/optimize-images.sh`.

3. Smoke test: create a throwaway test image, run the script into a temp dir, confirm output exists and `magick identify -verbose` (or `exiftool` if present) shows no GPS/EXIF. Delete the throwaway files. No real book images exist this phase (per 01-RESEARCH.md Open Question #3) — the script is committed now so every later phase reuses it unchanged.

4. Final foundation smoke test: `mdbook clean src && mdbook build src` → exits 0. Confirm `docs/` was produced at repo root (build-dir="../docs").
  </action>
  <verify>`bash scripts/optimize-images.sh <tmpin> <tmpout>` produces a resized JPEG with stripped metadata; `mdbook build src` exits 0 and `docs/index.html` exists.</verify>
  <done>Executable optimize-images.sh strips EXIF/GPS + resizes; full book builds green into docs/.</done>
</task>

</tasks>

<verification>
- `mdbook build src` exits 0 (no missing-file errors from SUMMARY draft or real links).
- `mdbook serve src` shows Korean text; custom.css applies keep-all and Noto Sans KR (or system Korean fallback).
- SUMMARY.md sidebar shows all 6 parts; 2부/3부/4부/부록 entries are greyed drafts.
- scripts/optimize-images.sh is executable and strips EXIF/GPS.
</verification>

<success_criteria>
Phase Success Criteria #1 (Korean local build/preview) and #2 (image pipeline + chapter-matched folder structure) are met. Downstream content plans can edit stub files without touching SUMMARY.md or breaking the build.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mdbook-뼈대-장비-기초-촬영법/01-01-SUMMARY.md`.
</output>
