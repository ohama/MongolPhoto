---
phase: 01-mdbook-뼈대-장비-기초-촬영법
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
autonomous: false
files_modified:
  - .github/workflows/mdbook.yml
  - docs/
  - README.md

must_haves:
  truths:
    - "main에 push하면 GitHub Actions가 mdBook을 빌드하고 docs/를 커밋·푸시한다"
    - "GitHub Pages가 'Deploy from a branch (main, /docs)'로 설정되어 사이트가 공개된다"
    - "지인이 https://ohama.github.io/MongolPhoto/ 링크로 장비·기초·템플릿 챕터 전체를 한국어로 탐색할 수 있다"
    - "배포 사이트에서 한국어가 Noto Sans KR/시스템 폰트로 렌더되고 사이드바 6부 목차가 깨짐 없이 보인다"
  artifacts:
    - path: ".github/workflows/mdbook.yml"
      provides: "PUB-02 CI: peaceiris/actions-mdbook@v2로 mdbook build src → docs/ 커밋·푸시 (/pages 스킬 패턴)"
      contains: "peaceiris/actions-mdbook"
    - path: "docs/index.html"
      provides: "CI가 생성한 배포 산출물"
    - path: "README.md"
      provides: "GitHub Pages 링크"
      contains: "github.io"
  key_links:
    - from: ".github/workflows/mdbook.yml"
      to: "docs/ (main 브랜치)"
      via: "build 후 git commit + push (build-and-commit-to-branch 패턴)"
      pattern: "mdbook build src"
    - from: "GitHub Pages 설정"
      to: "docs/ on main"
      via: "Deploy from a branch, /docs"
      pattern: "docs"
---

<objective>
Wire up and verify GitHub Pages deployment so the fully-populated Phase 1 book is publicly viewable by a friend via link.

Purpose: Satisfies Phase Success Criterion #1's deployment half (PUB-02) — GitHub Actions auto-deploys to Pages. Runs last so the checkpoint verifies the site with real gear/fundamentals/template content, not stubs.
Output: A committed CI workflow (build → commit docs/ to branch), Pages configured for Deploy-from-branch main /docs, a README link, and a human-verified live site.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mdbook-뼈대-장비-기초-촬영법/01-RESEARCH.md
@.claude/commands/pages.md

DECISION LOCKED (01-RESEARCH.md Pitfall #1 + Open Question #1): use the repo's `/pages` skill build-and-commit-to-branch pattern — NOT the OIDC actions/deploy-pages pattern. Pages Source MUST be "Deploy from a branch", Branch: main, Folder: /docs. Do not mix the two patterns. The `/pages` skill's workflow triggers on push to main/master and commits docs/ back via github-actions[bot]. mdBook project dir is `src` (so `mdbook build src`, build-dir="../docs" → repo-root docs/). git origin: https://github.com/ohama/MongolPhoto.git → Pages URL https://ohama.github.io/MongolPhoto/.
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI 워크플로 + README 링크 + push + Pages 설정 (PUB-02)</name>
  <files>.github/workflows/mdbook.yml, README.md, docs/</files>
  <action>
1. Create `.github/workflows/mdbook.yml` using the `/pages` skill Step 7 single-dir template (build-and-commit pattern), with {DIR}=src:
   - name: Build mdBook; on push branches [main, master] paths ['src/**']; also workflow_dispatch.
   - concurrency group mdbook-build, cancel-in-progress.
   - job: runs-on ubuntu-latest, permissions contents:write.
   - steps: checkout@v4 (submodules:false); peaceiris/actions-mdbook@v2 mdbook-version:'latest'; run `mdbook clean src && mdbook build src`; `touch docs/.nojekyll`; check-for-changes step (`git add docs/`; `git diff --cached --quiet || echo "changes=true" >> $GITHUB_OUTPUT`); commit+push step gated on changes==true using github-actions[bot] identity, commit msg "docs: rebuild mdBook site".
   (Verbatim structure is in .claude/commands/pages.md Step 7 — adopt it; only substitute {DIR}=src.)

2. `README.md`: add/ensure a `## Documentation` section linking `[몽골 은하수 촬영기](https://ohama.github.io/MongolPhoto/)` per the `/pages` skill Step 8. Create README.md if absent (H1 + Documentation section).

3. Ensure local docs/ is current: `mdbook clean src && mdbook build src && touch docs/.nojekyll`. Commit workflow + README + docs/.

4. Push to main. Configure Pages via gh CLI (automation-first): 
   `gh api -X POST repos/ohama/MongolPhoto/pages -f 'source[branch]=main' -f 'source[path]=/docs'` (if it already exists use `-X PUT`/`PATCH`; if it 404s on build_type, fall back to `gh api --method POST repos/ohama/MongolPhoto/pages -f build_type=legacy -f 'source[branch]=main' -f 'source[path]=/docs'`).
   If gh is unauthenticated or lacks the pages scope, do NOT hand this to the user as manual work beyond the settings toggle — surface it in the checkpoint below.

5. Confirm the Actions run: `gh run list --workflow=mdbook.yml` then `gh run watch` (or poll) until the build job succeeds and the bot commit to docs/ lands.
  </action>
  <verify>`.github/workflows/mdbook.yml` contains `peaceiris/actions-mdbook` and `mdbook build src`; `gh run list` shows a successful mdbook.yml run; README.md contains the github.io link; Pages API returns source branch=main path=/docs.</verify>
  <done>CI builds on push and commits docs/; Pages is set to Deploy-from-branch main /docs; a successful run exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>GitHub Actions builds the mdBook site and commits docs/ on push to main; GitHub Pages serves it from main /docs at https://ohama.github.io/MongolPhoto/.</what-built>
  <how-to-verify>
1. Open https://ohama.github.io/MongolPhoto/ in a browser (allow ~1-2 min after the Actions run + Pages build; if 404, re-check Settings → Pages → Source = "Deploy from a branch", Branch: main, Folder: /docs).
2. Confirm the landing page (소개) and left sidebar show all 6 parts, with 1부 (장비 가이드 + 촬영 기초 + 연습 로그 템플릿) as clickable links and 2부/3부/4부/부록 greyed out.
3. Click into 장비 → 카메라 & 렌즈, 촬영 기초 → 500/NPF 룰, and 연습 로그 템플릿 — confirm Korean text renders cleanly (no tofu boxes, natural line-breaks / keep-all) and the pages have real content (not "작성 예정" stubs).
4. Confirm the site is reachable by someone who is not you (share the link, or open in a private window / logged-out) — a friend must be able to view it.
  </how-to-verify>
  <resume-signal>Type "approved" if the live site shows the full Korean Phase 1 content and is publicly viewable, or describe what is broken (404, missing pages, font/tofu issues, stub content still showing).</resume-signal>
</task>

</tasks>

<verification>
- mdbook.yml exists and matches the /pages build-and-commit pattern; a successful Actions run committed docs/.
- Pages Source = Deploy from a branch, main, /docs.
- Live site at https://ohama.github.io/MongolPhoto/ shows full Phase 1 Korean content, publicly viewable.
</verification>

<success_criteria>
Phase Success Criterion #1 (deployment half) met: mdBook auto-deploys via GitHub Actions to GitHub Pages and a friend can view the full Phase 1 book by link.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mdbook-뼈대-장비-기초-촬영법/01-05-SUMMARY.md`.
</output>
